import { createApp } from "vue"; // Importing createApp function from vue
import "./style.css"; // Importing the global css file
import App from "./App.vue"; // Importing the App component

createApp(App).mount("#app"); // Mounting the App component to the div with id app
